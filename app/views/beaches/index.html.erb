<h1>Beaches</h1>

<% @beaches.each do |beach| %>
  <div class = "beach_container">
    <%= link_to beach.name, beach_path(beach) %>
  </div>
  <hr>
<% end %>

<%= paginate @beaches %>

<% if user_signed_in? %>
  <p>
    <%= link_to "Create Beach", new_beach_path %>
  </p>
<% end %>
