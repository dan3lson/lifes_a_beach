<% if current_user.role == 'admin' %>
  <ul>
    <% @users.each do |user| %>
      <li><%= user.email %> (<%= link_to "delete this user", user_path(user), id: "delete-#{user.id}", method: :delete %>)</li>
      <br>
    <% end %>
  </ul>
<% end %>
